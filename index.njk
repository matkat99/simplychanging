---
layout: layouts/home.njk

eleventyNavigation:
  key: Home
  order: 1
---

{% set maxPosts = collections.teamPosts.length | min(3) %}
{# <h1>Latest {% if maxPosts == 1 %}Post{% else %}{{ maxPosts }} Posts{% endif %}</h1> #}
{% set postslist = collections.teamPosts | head(-5) %}
{% set postslistCounter = collections.teamPosts | length %}
{% include "interest-boxes.njk" %}
<section>
  {% include "postslist.njk" %}
  <p>More posts can be found in
    <a class="yellow" href="{{ '/posts/' | url }}">the archive</a>.</p>
</section>
